.git
node_modules
test-results
playwright-report
# The build output. Missed, a locally built artifact joins the build context and is then
# overwritten by the build inside the image — harmless, but transferred for nothing every
# time.
dist
# The demo site's artifacts, same reasoning: `npm run site` inside the image regenerates
# them.
site/frond
