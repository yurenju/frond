<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">

<!--
  frond 的字型綁定。

  兩件事在這裡被釘死：

  1. generic family（serif / sans-serif）解析到哪個字型。書自己的樣式表大量
     使用 serif / sans-serif，若解析順序隨基底映像浮動，斷行與斷頁就跟著浮動。

  2. 區域字面（TC / SC / JP）如何選用。Noto CJK 是一個家族，但因為漢字統一，
     「骨」「直」這類共用碼位在 TC / SC / JP 有不同字形，OTC 內實際裝著
     Noto Serif CJK TC / SC / JP 等多個字面。取到哪一個取決於字面選擇，而字面
     選擇通常由文件的 lang 加上 fontconfig 的語言比對決定——那正是三家瀏覽器
     可能各做各的地方。這裡顯式綁定，不讓瀏覽器自己決定。

  這件事的危險性與 vert / vrt2 同一類：全形漢字多半等寬，選錯區域字面不一定
  會改變斷行，於是幾何不變量可能全數通過而畫面上的字形是錯的。

  處理順序有意義，而且有兩層：

  1. 檔名層。fontconfig 依檔名順序處理 /etc/fonts/conf.d/，而 mode="prepend"
     是把家族插到最前面，所以「檔名較後」等於「優先權較高」。基底映像的
     fontconfig-config 帶有 60-latin.conf，它同樣對 serif / sans-serif 做
     prepend（Noto Serif、DejaVu Serif 之類）。本檔若編號小於 60，會被它蓋掉，
     fc-match serif 解析到的是拉丁字型而不是 Noto CJK——所以編號取 70。

     副作用要知道：拉丁文字也會落到 Noto CJK 的拉丁字符上。對這個測試環境是
     可接受的，甚至是想要的——一個字型一個事實來源，三家瀏覽器沒有各自
     fallback 的空間。

  2. 檔案內順序。同一檔內同樣是後者蓋前者，所以通則寫在前，語言特化寫在後。
-->
<fontconfig>

  <!-- ===================================================================
       通則：無 lang 資訊時的預設字面
       ===================================================================
       必須有一個確定的答案，否則「沒宣告 lang 的書」會落回基底映像的預設
       解析順序，那正是要擋掉的東西。專案以繁體中文為主，預設取 TC。
  -->
  <match target="pattern">
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK TC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK TC</string>
    </edit>
  </match>

  <!-- 直排書常見的 monospace 宣告也一併綁定，避免落到無直排字符的等寬字型 -->
  <match target="pattern">
    <test name="family"><string>monospace</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans Mono CJK TC</string>
    </edit>
  </match>

  <!-- ===================================================================
       日文
       =================================================================== -->
  <match target="pattern">
    <test name="lang" compare="contains"><string>ja</string></test>
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK JP</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>ja</string></test>
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK JP</string>
    </edit>
  </match>

  <!-- ===================================================================
       繁體中文
       ===================================================================
       zh-tw 與 zh-hant 都要列。書宣告的是哪一種不由我們決定，只列其中一種
       的話另一種會靜默落回上面的預設規則——那正是「交給各家瀏覽器自己的
       語言比對」的漏洞，只是往下挪了一層。
  -->
  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-tw</string></test>
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK TC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-tw</string></test>
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK TC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-hant</string></test>
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK TC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-hant</string></test>
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK TC</string>
    </edit>
  </match>

  <!-- ===================================================================
       簡體中文
       =================================================================== -->
  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-cn</string></test>
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK SC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-cn</string></test>
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK SC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-hans</string></test>
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Serif CJK SC</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="lang" compare="contains"><string>zh-hans</string></test>
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Noto Sans CJK SC</string>
    </edit>
  </match>

</fontconfig>
